<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LogisticsMobile.LoginPage">
    <ContentPage.Content>
        <StackLayout Padding="15">
            <StackLayout Orientation="Horizontal">
                <Label Text="Фамилия" HorizontalOptions="StartAndExpand"/>
                <Entry Placeholder="напр.Иванов" Text="{Binding Family}" WidthRequest="100"/>
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="Имя" HorizontalOptions="StartAndExpand"/>
                <Entry Placeholder="напр.Иван" Text="{Binding Name}" WidthRequest="100"/>
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="Пароль" HorizontalOptions="StartAndExpand"/>
                <Entry IsPassword="True" Text="{Binding Password}" WidthRequest="100"/>
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="сохранить данные для входа" HorizontalOptions="StartAndExpand"/>
                <Switch IsToggled="{Binding IsStayLogin}" />
            </StackLayout>
            <Button Text="Нажать" Command="{Binding LoginButtonCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>